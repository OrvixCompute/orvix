[pytest]
# Only collect the real unit-test suite. The root-level test_*.py files are
# manual end-to-end helper scripts (they need a running server / extra deps).
testpaths = tests
asyncio_mode = auto
