# Core runtime dependencies (CPU-only — works on any machine for development).
# For real GPU inference install the extras: `pip install -e .[gpu]` (Linux/CUDA).
click>=8.1
loguru>=0.7
pyyaml>=6.0
pydantic>=2.7
pydantic-settings>=2.3
fastapi>=0.110
uvicorn>=0.29
websockets>=12.0
httpx>=0.27
tiktoken>=0.7

# Tests
pytest>=8.0
pytest-asyncio>=0.23
